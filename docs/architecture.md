# Architecture Notes

The repository is intentionally small while keeping boundaries clear:

- shared domain logic in reusable packages
- orchestration and validation in API service
- dual UI surfaces (web + desktop)
- pluggable event adapters
- explicit defect toggle model for reproducible exercises
